apiVersion: v1
kind: Service
metadata:
  name: udagram
  labels:
    service: udagram
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 8100
    nodePort: 30001
  selector:
    app: udagram-frontend
  type: LoadBalancer